import { Bookmarked, Like, Post, SeenBy, TaggedUser, User } from "@prisma/client";
import { api, RouterOutputs } from "~/utils/api";

// export type ExpandedPost = RouterOutputs["post"]["getPostById"]
